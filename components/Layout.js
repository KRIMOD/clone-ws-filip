const Layout = ({ children }) => {
  return (
    <div className='font-sans text-gray-900 flex items-center justify-center max-w-screen-lg m-auto md:px-16 sm:px-2'>
      {children}
    </div>
  )
}

export default Layout
